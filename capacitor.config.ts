import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nsu.book.exchange',
  appName: 'NSU Book Exchange',
  webDir: 'dist'
};

export default config;
